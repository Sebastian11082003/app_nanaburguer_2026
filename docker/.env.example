# Copy to docker/.env and adjust before a public host.
# Defaults in docker-compose.yml are for a local MVP only.

POSTGRES_PASSWORD=nanaburguer
JWT_SECRET=change-me-local-mvp-only
NEXT_PUBLIC_API_URL=http://localhost:3000
CORS_ORIGINS=http://localhost,http://localhost:80,http://127.0.0.1
ALLOW_INSECURE_DEFAULTS=true
SEED_ON_BOOT=true
PLATFORM_ADMIN_EMAIL=admin@nanaburger.com
PLATFORM_ADMIN_PASSWORD=123456
